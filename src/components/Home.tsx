import Layout from "@/components/system/Layout";
import { Button, Navbar, Text } from "@nextui-org/react";

const Home = () => {
  return (
    <Layout>
      <Text>home</Text>
    </Layout>
  );
};

export default Home;
